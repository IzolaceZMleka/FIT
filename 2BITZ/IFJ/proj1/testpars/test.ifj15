int f(int param1);
int main(){
	auto s = "text";
	int i = 0;
	double f;
	i = find(s,s);
	if (i>0)
		{return 1;}
	else
		{cin >> s;}
	for(int i = 1; i > 0; i=i-1)
		{cout << s;}
}
int f(int param1){
	return 1;
}